import LayoutComponent from '../../components/layout/layout.component';
import TabIcon from '../../navigation/private/config/main.tab';

const MainScreen = () => {
  return (
    <LayoutComponent>
      <TabIcon />
    </LayoutComponent>
  );
};

export default MainScreen;
